{
  "name": "easypost-mobile-proxy",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "dev:init": "wrangler d1 execute easypost-mobile --local --file=schema.sql",
    "deploy": "wrangler deploy",
    "test": "node --test"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
