<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!--
      Hardened-runtime entitlements required to notarize a PyInstaller/PySide6
      app. Python's interpreter allocates executable memory, and the bundle
      ships its own dylibs/frameworks that aren't all signed by us, so the
      standard library-validation and executable-memory protections must be
      relaxed. These are the minimum needed; no privacy-sensitive entitlements
      are requested.
    -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
</dict>
</plist>
